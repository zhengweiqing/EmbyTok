name: Android Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ANDROID_COMPILE_SDK: "34"
      ANDROID_BUILD_TOOLS: "34.0.0"
      ANDROID_SDK_ROOT: ${{ github.workspace }}/android-sdk
      CMDLINE_TOOLS_VERSION: "11076708"
      # 指定Gradle版本（根据项目实际需求调整，比如8.5）
      GRADLE_VERSION: "8.5"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        # 确保拉取完整仓库（默认浅克隆可能不影响，但兜底）
        with:
          fetch-depth: 1

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Install Android SDK components
        run: |
          sudo apt-get update && sudo apt-get install -y unzip
          mkdir -p $ANDROID_SDK_ROOT/cmdline-tools
          curl -fL --retry 3 \
            "https://dl.google.com/android/repository/commandlinetools-linux-${CMDLINE_TOOLS_VERSION}_latest.zip" \
            -o cmdline-tools.zip
          unzip -t cmdline-tools.zip || (echo "ZIP损坏！" && exit 1)
          unzip -q cmdline-tools.zip -d $ANDROID_SDK_ROOT/cmdline-tools
          mv $ANDROID_SDK_ROOT/cmdline-tools/cmdline-tools $ANDROID_SDK_ROOT/cmdline-tools/latest
          
          export ANDROID_HOME=$ANDROID_SDK_ROOT
          export PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH
          
          which sdkmanager || (echo "sdkmanager未找到！" && exit 1)
          sdkmanager --version
          
          mkdir -p ~/.android/
          echo "https://mirrors.tuna.tsinghua.edu.cn/android/repository/" > ~/.android/repositories.cfg
          yes | sdkmanager --licenses
          sdkmanager --verbose "platforms;android-$ANDROID_COMPILE_SDK" "build-tools;$ANDROID_BUILD_TOOLS"
          
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH" >> $GITHUB_ENV

      - name: 修复Gradle Wrapper（核心步骤）
        run: |
          # 1. 检查现有Wrapper文件（若缺失则重建）
          if [ ! -f "gradle/wrapper/gradle-wrapper.jar" ]; then
            echo "=== 重建Gradle Wrapper ==="
            # 删除无效的wrapper配置
            rm -rf gradle/wrapper
            # 用SDKMAN安装指定版本Gradle，生成新的Wrapper
            curl -s "https://get.sdkman.io" | bash
            source "$HOME/.sdkman/bin/sdkman-init.sh"
            sdk install gradle $GRADLE_VERSION
            # 在项目根目录生成Wrapper
            gradle wrapper --gradle-version $GRADLE_VERSION --distribution-type bin
          fi

          # 2. 强制赋予gradlew执行权限（兜底）
          chmod +x ./gradlew

          # 3. 验证Wrapper完整性
          echo "=== 验证Gradle Wrapper ==="
          ./gradlew --version || (echo "Wrapper仍异常！" && exit 1)

      - name: Build with Gradle
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
